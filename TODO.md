# TODO

- [ ] Update `frontend/src/App.tsx` with routes + layout per `frontend/TODODAWN.md`.
- [ ] Add public pages: `AuthorPage`, `ArticlePage`, `TrendingPage`.
- [ ] Add private page: `LearningDagPage`.
- [ ] Add core components: `Header`, `SearchComponent`, `InOrderComponent`, `PreviewListComponent`, `PreviewComponent`, `AuthorComponent`, `AuthorArticlesComponent`, placeholder `ArticlesDagComponent`.
- [ ] Add private components: `InteractionComponent` and `MyInteractionsComponent`.
- [ ] Wire mutations via `api.ts` and call `rule.refresh` after mutation.
- [ ] Ensure every component is wrapped with `f.observer` and all reads go through `rule.useResolve`.
- [ ] Run frontend build/tests to validate.

