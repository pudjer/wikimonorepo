# Frontend TODO

## Learning DAG Stats Component

- [x] Create `LearningDagStats.tsx` stub component
- [ ] Add `dagStore` to `ProfilePage` and fetch DAG based on learnProgress
- [ ] Build `StatsBuilder` from `learnProgress` + `dagStore.dag` links wrapped in `NodeRelations`
- [ ] Render `<LearningDagStats />` in ProfilePage

